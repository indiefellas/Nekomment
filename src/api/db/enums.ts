
export enum PostBehavior {
    AutoPublish = 0,
    HoldForReview = 1,
}

export enum AutoModType {
    KeywordList = 0,
    Regex = 1,
}

export enum AutoModBehavior {
    Block = 0,
    HoldForReview = 1,
}